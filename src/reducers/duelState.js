
const initialState = {
  states: [],
};

const duelState = (state = initialState, action) => {
  switch (action.type) {
    case 'Add_ST': 

    break;
    
    default:
      return state;
  }
};

export default duelState;